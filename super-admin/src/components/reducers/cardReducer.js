
const cardReducerDefaultState = [],

const cardReducer = (state = cardReducerDefaultState, action) => {
  switch (action.type) {
    case 'ADD_CARD':

      break;
    default:
      return state;

  }
}
